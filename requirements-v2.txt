# v2 stack is now included in requirements.txt (Streamlit Cloud / single pip install).
# This file is kept for documentation and explicit reinstall:
#   pip install -r requirements-v2.txt
# No LLM / no spaCy statistical models — blank English + EntityRuler only.

docling>=2.0
whoosh>=2.7
spacy>=3.7
